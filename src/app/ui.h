#pragma once
#include <windows.h>

// Cria (se ainda não criada) e exibe a janela principal de configurações
// Retorna o HWND da janela criada/existente
HWND UI_Show(HINSTANCE hInstance, HWND owner);




